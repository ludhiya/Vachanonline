import React from "react";

const aboutUs = props => {
  return (
    <div className="aboutWebsite">
      <div className="aboutText">{props.aboutUs}</div>
    </div>
  );
};

export default aboutUs;
